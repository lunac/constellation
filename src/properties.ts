export const git = {
    error: {
        notGitRepository: {
            en: "not a git repository (or any of the parent directories): .git",
        },
        noStagedFiles: {
            en: 'not staged files to commit!',
        }
    }
};